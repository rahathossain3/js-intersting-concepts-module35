function add(first, second) {
    const result = first + second;
    return result;
}
const sum = add(11, 35)
console.log(first);

if (result > 9) {
    // var mode = 'happy';
    mode = 'happy';     //global licking
    console.log(mode);
}
else {
    console.log(mode, 'is off')
}